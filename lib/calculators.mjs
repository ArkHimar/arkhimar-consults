export function roi(totalBenefits,totalCosts){return totalCosts===0?null:(totalBenefits-totalCosts)/totalCosts}
export function npv(rate,cashFlows){if(rate<=-1)throw new RangeError('Discount rate must be greater than -1');return cashFlows.reduce((value,cashFlow,period)=>value+cashFlow/(1+rate)**period,0)}
export function payback(cashFlows){let cumulative=0;for(let period=0;period<cashFlows.length;period++){const previous=cumulative;cumulative+=cashFlows[period];if(cumulative>=0){if(period===0)return 0;const flow=cashFlows[period];return flow===0?period:period-1+Math.abs(previous)/flow}}return null}
export function evm({pv,ev,ac,bac}){const safe=(numerator,denominator)=>denominator===0?null:numerator/denominator;const cpi=safe(ev,ac);return{cv:ev-ac,sv:ev-pv,cpi,spi:safe(ev,pv),eac:cpi===null||cpi===0?null:bac/cpi,vac:cpi===null||cpi===0?null:bac-bac/cpi}}
export function riskScore(probability,impact){if(probability<0||impact<0)throw new RangeError('Risk inputs cannot be negative');return probability*impact}
export function weightedScore(criteria){const weightTotal=criteria.reduce((sum,item)=>sum+item.weight,0);if(weightTotal===0)return null;return criteria.reduce((sum,item)=>sum+item.score*item.weight,0)/weightTotal}
export function calculateCpm(tasks,dependencies){const byId=new Map(tasks.map(t=>[t.id,{...t,es:0,ef:0,ls:0,lf:0,float:0}]));const incoming=new Map(tasks.map(t=>[t.id,[]]));const outgoing=new Map(tasks.map(t=>[t.id,[]]));for(const edge of dependencies){if(!byId.has(edge.from)||!byId.has(edge.to))throw new Error('Dependency references an unknown task');incoming.get(edge.to).push(edge.from);outgoing.get(edge.from).push(edge.to)}const queue=tasks.filter(t=>incoming.get(t.id).length===0).map(t=>t.id);const order=[];while(queue.length){const id=queue.shift();order.push(id);for(const next of outgoing.get(id)){if(incoming.get(next).every(parent=>order.includes(parent)))queue.push(next)}}if(order.length!==tasks.length)throw new Error('Dependency cycle detected');for(const id of order){const task=byId.get(id);task.es=Math.max(0,...incoming.get(id).map(parent=>byId.get(parent).ef));task.ef=task.es+task.duration}const finish=Math.max(0,...[...byId.values()].map(t=>t.ef));for(const id of [...order].reverse()){const task=byId.get(id);task.lf=outgoing.get(id).length?Math.min(...outgoing.get(id).map(next=>byId.get(next).ls)):finish;task.ls=task.lf-task.duration;task.float=task.ls-task.es}return{duration:finish,tasks:order.map(id=>byId.get(id)),critical:order.filter(id=>byId.get(id).float===0)}}
