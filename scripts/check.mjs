import {access,readFile} from 'node:fs/promises';
const pages=['index.html','start-a-project/index.html','project-management/index.html','pm/index.html'];const required=['styles.css','app.js','assets/images/hero-architecture.jpg','assets/images/studio-drawings.jpg','assets/images/project-duplex.jpg','assets/images/project-commercial.jpg','assets/images/project-interior.jpg'];let errors=[];
for(const file of [...pages,...required]){try{await access(file)}catch{errors.push(`Missing ${file}`)}}for(const page of pages){const html=await readFile(page,'utf8');if(!/<title>[^<]+<\/title>/.test(html))errors.push(`${page}: missing title`);if(!/<meta name="description"/.test(html))errors.push(`${page}: missing description`);if(!/<main(?:\s|>)/.test(html))errors.push(`${page}: missing main landmark`)}const enquiry=await readFile('start-a-project/index.html','utf8');if(!/enctype="multipart\/form-data"/.test(enquiry))errors.push('Project form must use multipart encoding');if(!/<input[^>]+type="file"[^>]+multiple/.test(enquiry))errors.push('Project form must support multiple attachments');if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log(`Checked ${pages.length} routes, ${required.length} shared assets and the attachment form contract.`);
