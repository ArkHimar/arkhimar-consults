{
  "name": "arkhimar-consults",
  "private": true,
  "version": "1.0.0",
  "description": "ArkHimar Consult public website and ArkHimar PM foundation",
  "scripts": {
    "dev": "npm run build && node scripts/serve.mjs dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
